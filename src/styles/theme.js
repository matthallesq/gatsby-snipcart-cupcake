// Here a theme is defined. This theme is passed to the entire site (via layout.js) so the individual parts of the theme object
// can be accessed in each styled component.

export const theme = {
    colors: {
        main: '#E2C391', //used in header
        primaryAccent: '#F6E27F', //used for thumbnails on homepage and as price background
        secondaryAccent: '#9BBEC7', //used for header border and item buttons
    }
};
